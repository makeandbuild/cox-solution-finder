extends ../layouts/default
//- Use the Companion Site Default Template

block content
	h1 Industry Page
	if !data.industry
		h2 Invalid Post.
	else
		h2 True! Now what?
		div= data.industry.title
		div= data.industry.heading
		div= data.industry.content
